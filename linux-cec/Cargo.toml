[package]
name = "linux-cec"
version = "0.1.0"
edition = "2021"

[lib]

[dependencies]
bitfield = "0.17"
bitflags = "2.6"
nix = { version = "0.29", default-features = false, features = ["ioctl"] }
num_enum = "0.7"
linux-cec-macros = { path = "../linux-cec-macros" }
