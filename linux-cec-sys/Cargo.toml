[package]
name = "linux-cec-sys"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = []

[lib]

[dependencies]
bitflags = "2.6"
nix = { version = "0.29", default-features = false, features = ["ioctl"] }
